version: "3.9"

services:
  ai-interview-agent:
    build: .

    container_name: ai-interview-agent

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./data:/app/data

    restart: unless-stopped